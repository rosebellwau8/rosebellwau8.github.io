{
  "name": "blog",
  "version": "1.0.0",
  "private": true,
  "description": "Personal blog — pure HTML posts, zero-framework build",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "publish": "node scripts/publish.js",
    "test": "node --test",
    "check": "node --check scripts/build.js && node --test"
  }
}
