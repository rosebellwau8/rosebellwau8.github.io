# 个人博客项目审计报告

> 审计日期：2026-07-31  
> 审计范围：项目根目录下的源代码、文章源文件、构建产物、说明文档及本地浏览器表现  
> 审计方式：静态代码审阅、实际构建、哈希一致性检查、站内链接检查、边界行为验证、Playwright 桌面/移动端检查

## 1. 结论摘要

这是一个目标明确、结构克制的零依赖静态博客生成器。它采用完整 HTML 文章作为内容源，通过一段 Node.js 脚本生成首页和标签归档页，再把结果发布到 `docs/`。对于“个人维护、内容可信、规模较小”的使用场景，当前版本已经具备可用的 MVP 能力。

综合判断：

- **声明范围内的 MVP 完成度：约 85%～90%**。文章扫描、日期倒序、标签页、原样复制、公共样式、GitHub Pages 发布目录均已落地。
- **面向长期稳定运行的工程完成度：约 55%～60%**。主要欠缺输入校验、路径安全、HTML 转义、失败保护、自动化测试和持续集成。
- **当前可用性：可用于可信作者维护的个人博客**。现有 2 篇文章、2 个标签可正常构建、浏览和导航。
- **当前不适合的场景：多人投稿、自动接收外部内容、无人值守发布、文章量快速增长**。这些场景会放大标签路径、元数据解析和构建失败保护方面的问题。

本次没有修改业务实现，仅运行了构建并新增本报告。构建产物与源文件保持一致。

## 2. 项目定位与架构

### 2.1 当前功能

项目提供以下能力：

1. 从 `posts/*.html` 扫描文章。
2. 从文章 `<meta>` 标签中读取标题、日期和标签。
3. 按日期字符串倒序排列文章。
4. 生成 `docs/index.html` 首页文章列表。
5. 按标签生成 `docs/tags/*.html` 归档页。
6. 将文章逐字节复制到 `docs/posts/`。
7. 将公共 CSS 复制到 `docs/assets/`。
8. 使用 `docs/` 作为 GitHub Pages 发布目录。

### 2.2 数据流

```text
posts/*.html ──扫描/正则提取元数据──> posts 内存模型
                                         │
                                         ├──日期排序──> docs/index.html
                                         ├──标签聚合──> docs/tags/*.html
                                         └──原样复制──> docs/posts/*.html

docs-src/site.css ────────────────────────────────────> docs/assets/site.css
```

整体架构非常轻，构建时没有第三方依赖，发布结果也没有客户端 JavaScript 或外部资源请求。这带来了良好的可理解性、加载性能和较低的供应链风险。

## 3. 目录与实现概况

审计时项目共有 9 个非构建产物文件，约 13.6 KB、456 行；核心构建脚本为 149 行。实际生成 6 个文件，总计约 9.97 KB：

| 路径 | 作用 | 状态 |
|---|---|---|
| `posts/` | 完整 HTML 文章源文件，当前 2 篇 | 已实现 |
| `scripts/build.js` | 唯一构建入口 | 已实现 |
| `docs-src/site.css` | 首页和标签页公共样式 | 已实现 |
| `docs/` | 静态发布产物 | 已生成 |
| `package.json` | 提供 `build` 命令，零依赖 | 已实现 |
| `README.md` | 写作、构建、Pages 部署说明 | 基本完成 |
| `PROGRESS.md` | 任务进展记录 | 有记录，但部分结论偏乐观 |
| `BLOCKED.md` | 阻塞记录 | 当前无阻塞 |
| `.gitignore` | 忽略 `node_modules/` | 已实现 |

当前目录不是 Git 工作区；执行 `git status` 返回“not a git repository”。因此无法审计提交历史、分支策略、GitHub Actions 或实际的 GitHub Pages 仓库设置。

## 4. 功能完成度评估

| 能力 | 完成度 | 评价 |
|---|---:|---|
| HTML 文章发现 | 90% | 能扫描小写 `.html`；不支持 `.HTML`，目录缺失时直接异常 |
| 元数据读取 | 65% | 文档规定的精确格式可用；合法但属性顺序不同、单引号或额外属性会解析失败 |
| 首页生成 | 90% | 当前数据输出正确；缺少空状态和动态文本转义 |
| 日期倒序 | 75% | ISO 日期正常；无日期合法性校验、同日文章无显式次级排序 |
| 标签归档 | 70% | 普通中文标签正常；特殊对象键、路径字符和保留文件名可能导致崩溃或越界写入 |
| 文章原样复制 | 100% | SHA-256 验证为逐字节一致 |
| 公共样式复制 | 90% | 正常存在时一致；缺失时构建仍成功但页面样式链接失效 |
| 重复构建一致性 | 100%（正常输入） | 连续构建两次，文件集合和所有 SHA-256 均不变 |
| 响应式浏览 | 90% | 1440px 和 375px 均无横向溢出，页面清晰可用 |
| 站内导航 | 100%（当前内容） | 首页、文章、标签页之间导航均正常，所有本地 `href/src` 目标存在 |
| 自动化测试 | 10% | 没有 `test` 脚本、单元测试、集成测试或 CI |
| 发布准备 | 60% | `docs/` 和 README 步骤具备；没有可审计的 Git 仓库或自动部署配置 |
| SEO/订阅能力 | 25% | 只有基础 `<title>`；无 description、canonical、OG、RSS、sitemap 等 |

## 5. 已验证结果

### 5.1 构建与一致性

| 检查项 | 结果 |
|---|---|
| Node.js 版本 | v24.14.0 |
| npm 版本 | 11.9.0 |
| `node --check scripts/build.js` | 通过 |
| `npm run build` 等价命令 | 退出码 0 |
| 构建统计 | 2 篇文章、2 个标签 |
| 文章源文件与 `docs/posts/` 哈希 | 全部一致 |
| CSS 源文件与 `docs/assets/site.css` 哈希 | 一致 |
| 连续两次构建 | 文件集合一致，变更哈希数为 0 |
| 生成目录内部链接 | 全部存在 |

### 5.2 浏览器层验证

使用 Chromium headless 分别检查了桌面和移动端：

- 首页标题和 H1 均为“博客”。
- 首页识别到 2 个文章链接、2 个标签链接、1 个已加载样式表。
- 文章链接可进入对应文章，文章标题匹配，并存在返回首页链接。
- 标签链接可进入对应标签页，标签标题匹配，文章数量正确，并存在返回首页链接。
- 1440px 桌面视口和 375px 移动视口均无横向溢出。
- 没有空文本链接、控制台错误或页面运行时错误。
- 移动端排版能正常折行，正文宽度适配视口。

### 5.3 边界行为验证

除正常路径外，还针对现有实现做了等价内存测试：

- 文档规定的 `<meta name="title" content="OK">` 可以解析。
- 属性顺序反转、使用单引号、在两个属性之间增加额外属性时，标题均解析为空。
- 标签名为 `constructor` 时，当前普通对象聚合逻辑触发 `TypeError`。
- 标签名为 `../escape` 时，最终路径会从 `docs/tags/` 逃逸到 `docs/`。
- 标签名为 `../../README` 时，最终路径会逃逸到项目根目录。

这些结果证实相关问题不是纯理论推断。

## 6. 做得好的地方

### 6.1 目标克制，认知负担低

项目没有引入框架、模板引擎或第三方包。核心流程能在一个文件中读完，适合个人长期掌控，也避免了依赖升级和供应链维护成本。

### 6.2 源文章保持独立和完整

文章本身是完整 HTML，包含各自的版式和样式。构建脚本在 `scripts/build.js:52-55` 只做文件复制，不改写文章。哈希检查证明现有文章确实被逐字节复制。

### 6.3 正常输入下具备可重复构建性

`scripts/build.js:17-22` 会清理输出目录，当前输入下重复运行得到完全相同的文件集合和内容。这一点对静态部署很重要。

### 6.4 路径均为相对路径

首页、文章页和标签页使用相对链接，因此既可部署在用户主页仓库，也较容易迁移到子目录或本地打开。

### 6.5 页面轻量且响应式可用

当前全部发布文件不足 10 KB，不需要客户端脚本。CSS 结构简洁，移动端媒体查询有效，实际浏览器检查没有出现溢出或运行时错误。

## 7. 风险与缺陷

以下优先级按“发生后的影响”和“未来内容来源扩大后的风险”综合评定。当前只有可信作者时，安全风险的实际暴露面较小，但工程缺陷仍然存在。

### P1-1：标签名直接参与输出路径，缺少路径约束

位置：`scripts/build.js:139-143`

标签名被直接拼接为 `${tag}.html` 并交给 `path.join`。标签含 `/`、`\` 或 `..` 时，目标路径可能离开 `docs/tags/`；Windows 保留名或非法字符还会使构建失败。首页链接对标签使用了 `encodeURIComponent`，但磁盘文件名没有采用同一套安全映射。

影响：

- 构建过程可能向预期目录之外写入 `.html` 文件。
- 某些标签会导致构建中断。
- URL 编码规则和磁盘命名规则不一致，长期容易出现死链。

建议：

1. 将标签显示名与文件名分离，例如生成 `tag-${safeSlug}.html`。
2. 对所有标签统一 slug 化；必要时追加稳定短哈希避免重名。
3. 用 `path.resolve` 计算目标后强制检查其仍位于 `docs/tags/` 内。
4. 对 `/`、`\`、控制字符、Windows 保留名和空标签做显式校验。

### P1-2：生成 HTML 时没有转义动态内容

位置：`scripts/build.js:58-65`、`106-110`、`118`、`124`

文章标题、日期和标签被直接插入 HTML。标题或标签包含 `&`、`<`、`>`、引号或 HTML 片段时，会破坏文档结构，甚至把内容注入首页和标签页。

虽然文章源本身就是可信 HTML，这使当前安全影响有限，但生成页仍应把“文本元数据”按文本处理，而不是按 HTML 处理。

建议增加统一的 `escapeHtml()`，所有文本节点均转义；所有 URL 路径段使用独立的 URL 编码函数。不要用同一个函数同时处理 HTML 文本和 URL。

### P1-3：构建先删除旧产物，之后才暴露多数输入错误

位置：`scripts/build.js:40-55`、`96-143`

脚本在完成全部输入校验和标签路径计算之前就递归删除 `docs/`。如果后续因非法标签、写入失败或其他异常中断，最后一个可用版本会被替换为不完整产物。

建议采用两阶段构建：

1. 先扫描、解析并验证全部文章，在内存中完成标签映射和目标路径计算。
2. 输出到临时目录。
3. 所有文件生成并通过链接检查后，再原子替换 `docs/`；替换失败时保留旧版本。

最低成本的改进是“先完整校验，再清理 `docs/`”，这已能消除大部分失败窗口。

### P1-4：标签聚合使用普通对象，特殊键会导致构建崩溃

位置：`scripts/build.js:97-103`

`tagMap = {}` 继承了 `Object.prototype`。当标签是 `constructor`、`__proto__` 等特殊键时，`tagMap[t]` 不是预期数组，随后调用 `.push()` 会失败。`constructor` 用例已经实际复现 `TypeError`。

建议使用 `new Map()`；若继续使用对象，至少使用 `Object.create(null)`，但 `Map` 的语义更清晰。

### P2-1：元数据解析器过度依赖固定字符串格式

位置：`scripts/build.js:8-10`

当前正则只接受以下精确约束：

- `name` 必须位于 `content` 前面；
- 两者必须相邻，中间不能有其他属性；
- 属性值必须使用双引号；
- 属性名和结构必须符合正则预期。

合法 HTML 并不保证这些条件。解析失败后，标题和日期会静默降级，标签会消失，因此错误不容易在发布前被发现。

在坚持零依赖的前提下，推荐两种方案：

- **低改动方案**：继续规定严格格式，但对每篇文章做完整校验，格式不符立即报出文件名、字段名并终止构建。
- **稳健方案**：把元数据放入同名 JSON sidecar 文件，如 `post.html` + `post.json`，直接使用 `JSON.parse`，彻底避免用正则解析 HTML 属性。

### P2-2：日期、必填字段和重复值没有校验

位置：`scripts/build.js:31-38`

缺失标题会回退到文件名，缺失日期会回退到 `1970-01-01`，非法日期也会原样参与字符串排序。这样虽然构建不容易失败，但更容易静默发布错误内容。同一文章中的重复标签也会重复进入归档页。

建议：

- 标题、日期设为必填；标签可选但必须为字符串数组或可验证列表。
- 日期要求严格 `YYYY-MM-DD`，并验证真实日历日期。
- 标签做去重、长度限制和规范化。
- 同日文章增加文件名或明确权重作为次级排序键，确保跨平台稳定。

### P2-3：必要资源缺失时仍报告构建成功

位置：`scripts/build.js:46-50`

`docs-src/site.css` 不存在时，脚本会跳过复制，却仍生成引用 `assets/site.css` 的页面并以成功状态退出。结果是构建“绿色”，线上页面却没有样式。

建议把 CSS 视为必需输入，缺失时立即失败；构建结束前运行一次内部链接和资源存在性检查。

### P2-4：文件名没有作为 URL 路径段编码

位置：`scripts/build.js:63`、`108`

文章文件名被直接写入 `href`。普通英文文件名没有问题，但空格、`#`、`%`、非 ASCII 或其他特殊字符可能产生歧义。建议在保持磁盘文件名不变的同时，对链接中的单个文件名使用 `encodeURIComponent`，并限制不安全文件名。

### P2-5：缺少自动化测试和 CI 质量门

位置：`package.json:6-8`

当前只有 `build` 命令。没有任何测试能在新增文章或修改脚本后自动阻止以下回归：元数据失效、顺序错误、标签路径异常、旧文件残留、内部死链、构建非幂等。

建议至少增加：

- 纯函数单元测试：`extractMeta/parseMeta`、`escapeHtml`、日期校验、标签 slug、路径包含检查。
- 临时目录集成测试：正常文章、空目录、缺字段、非法日期、特殊标签、特殊文件名。
- 产物测试：文章复制哈希相同、所有站内链接存在、连续构建结果一致。
- CI：执行语法检查、测试、构建；若 `docs/` 纳入版本控制，再检查构建后 `docs/` 无未提交差异。

Node 目前已内置 `node:test`，可以保持零第三方依赖。

### P2-6：发布状态说明与可验证事实有差距

`PROGRESS.md` 将“README 中存在部署步骤”记作“GitHub Pages 配置完成”，但当前目录没有 Git 元数据、Actions 工作流或可读取的远端 Pages 设置。因此更准确的状态应是“发布目录和部署说明已准备，实际仓库配置待验证”。

README 还应补充最低 Node.js 版本或 `package.json.engines`，避免在不支持 `fs.rmSync` 的旧环境中失败。

### P3-1：文本颜色对比度偏低

位置：`docs-src/site.css:24-30`、`54-58`、`75-80`

小字号文本使用 `#888`、`#999`、`#bbb`。它们在白色背景上的对比度约为 3.54:1、2.85:1、1.92:1，均低于 WCAG AA 对普通文本的 4.5:1 建议值。页脚尤其偏淡。

建议将辅助文本统一提高到至少约 `#666`，并保留清晰的键盘焦点样式。可进一步使用 `<time datetime>`、`<article>`、`<nav>` 等语义元素改善可访问性。

### P3-2：SEO 与内容发现能力较弱

首页和标签页只有基础标题，没有 description、canonical、Open Graph、结构化数据、RSS/Atom、sitemap 或 404 页面。对纯私人记录不是阻塞项，但如果目标是公开传播或搜索收录，应逐步补齐。

优先顺序建议为：每页 description 和 canonical → sitemap.xml → RSS/Atom → Open Graph。

### P3-3：内容样式完全内联，规模扩大后维护成本会上升

这是当前产品选择，不是错误。每篇文章拥有独立视觉风格很有特色；但当文章数量增加后，字体、移动端、代码块、可访问性和返回导航会重复维护。

可以保留文章自定义样式，同时提供一个可选的基础文章样式和文章模板，新文章只覆盖差异部分。

## 8. 推荐改进路线

### 第一阶段：保证构建边界安全

目标：任何坏文章都不能越界写文件，也不能破坏最后一个可用发布版本。

1. 拆出纯函数：读取元数据、转义 HTML、验证日期、生成安全 slug。
2. 使用 `Map` 聚合标签。
3. 对文章文件名和标签做白名单/规范化检查。
4. 所有输出路径做目录包含校验。
5. 所有动态文本做 HTML 转义，URL 路径段单独编码。
6. 先验证全部输入，再写临时目录并替换 `docs/`。

这是最高优先级，建议在增加更多文章或开放多人编辑前完成。

### 第二阶段：建立自动化质量门

目标：让当前已验证的正确行为可以自动重复验证。

1. 使用 `node:test` 建立单元和集成测试。
2. 增加 `npm test`、`npm run check`。
3. 把链接完整性、复制哈希、幂等性纳入测试。
4. 增加 GitHub Actions，push/PR 时执行检查。
5. 若提交 `docs/`，在 CI 中验证产物与源代码同步。

### 第三阶段：改善发布质量

目标：从“能打开”提升到“长期可维护、可发现、易阅读”。

1. 修正辅助文本对比度和语义标签。
2. 添加站点配置，如站名、描述、作者、基础 URL。
3. 生成 sitemap、RSS 和基础社交分享元数据。
4. 增加文章模板、草稿机制和本地预览命令。
5. 文章量明显增加后再考虑分页、全文搜索或按年归档，不建议提前引入大型框架。

## 9. 建议的目标构建结构

仍然可以保持零依赖和小体量：

```text
读取全部文章
  └─> 解析元数据
       └─> 严格校验（字段、日期、标签、文件名）
            └─> 生成安全且已转义的内存模型
                 └─> 输出到 docs.tmp/
                      ├─> 首页
                      ├─> 标签页
                      ├─> 文章副本
                      └─> CSS / sitemap / feed
                           └─> 链接与文件集合自检
                                └─> 原子替换 docs/
```

建议把 `scripts/build.js` 拆成“可导入的纯函数模块 + 很薄的命令行入口”。这样测试不必真的修改项目 `docs/`，也能精确覆盖每个边界条件。

## 10. 最终评价

该项目的核心思路是成立的，而且正常路径实现得干净：内容源简单、构建快速、输出轻量、现有页面可正常浏览，零依赖策略也与个人博客的定位一致。它现在不是“功能没做完”，而是“主路径已完成，保护栏尚未补齐”。

如果只由一个可信作者手工维护，可以继续使用；在下一轮开发中，应优先处理标签路径、HTML 转义、特殊对象键、输入校验和失败保护，再补自动化测试。完成前两阶段后，这个项目会从可用原型提升为一个体量仍然很小、但足够稳健的长期博客工具。
